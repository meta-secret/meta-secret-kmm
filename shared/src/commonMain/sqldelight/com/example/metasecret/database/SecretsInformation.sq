CREATE TABLE Secret (
    name TEXT NOT NULL,
    secretGuardType INTEGER NOT NULL,
    devicesCount TEXT NOT NULL
);

insertSecret:
INSERT INTO Secret(name, secretGuardType, devicesCount)
VALUES(?, ?, ?);

removeAllSecret:
DELETE FROM Secret;

selectAllSecret:
SELECT Secret.*
FROM Secret;